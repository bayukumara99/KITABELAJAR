(() => {
    const modal = document.querySelector('#accountModal');
    if (!modal) return;
    const trigger = document.querySelector('#accountButton') || document.querySelector('.nav-actions a:first-child');
    if (!trigger) return;
    let authMode = 'login';
    let accountRole = 'student';

    modal.innerHTML = `<div class="account-panel"><button class="forgot-close account-close" id="sharedCloseAccount" type="button" aria-label="Tutup halaman akun">x</button><div class="account-form" id="sharedAccountFormView"><div class="eyebrow">Your learning space</div><h2 id="sharedAccountTitle">Selamat datang kembali.</h2><p class="account-copy" id="sharedAccountCopy">Masuk untuk menyimpan produk dan melihat akses belajarmu dalam satu tempat.</p><div class="account-tabs"><button class="account-tab active" type="button" data-shared-auth="login">Masuk</button><button class="account-tab" type="button" data-shared-auth="signup">Buat akun</button></div><form id="sharedAccountForm"><label class="account-field" id="sharedNameField" hidden>Nama lengkap<input id="sharedAccountName" type="text" autocomplete="name" placeholder="Nama kamu"></label><label class="account-field">Email<input id="sharedAccountEmail" type="email" autocomplete="email" placeholder="nama@email.com" required></label><label class="account-field">Password<input id="sharedAccountPassword" type="password" autocomplete="current-password" minlength="6" placeholder="Minimal 6 karakter" required></label><button class="forgot-password" id="sharedForgotPassword" type="button">Lupa password?</button><p class="account-error" id="sharedAccountError" role="alert"></p><button class="account-submit" id="sharedAccountSubmit" type="submit">Masuk ke akun</button></form><p class="account-note">Demo frontend: data akun disimpan lokal di perangkat ini.</p></div><div class="account-dashboard" id="sharedAccountDashboard"><div class="dashboard-head"><div class="eyebrow">Personal dashboard</div><h2>Halo, <span id="sharedDashboardName"></span>.</h2><p>Ruang kecil untuk melanjutkan proses belajarmu.</p></div><div class="dashboard-body"><p class="dashboard-label">Akses kamu</p><div class="access-item"><strong>Weekly Reset Planner</strong><span>Siap dipakai</span></div><div class="access-item"><strong>Focus Without Force</strong><span>Siap dipakai</span></div><button class="logout" id="sharedLogout" type="button">Keluar dari akun</button></div></div></div>`;
    document.body.insertAdjacentHTML('beforeend', `<div class="forgot-panel" id="sharedForgotPanel" role="dialog" aria-modal="true" aria-labelledby="sharedForgotTitle"><div class="forgot-card"><button class="forgot-close" id="sharedCloseForgot" type="button" aria-label="Tutup lupa password">x</button><div class="eyebrow">Password recovery</div><h2 id="sharedForgotTitle">Lupa password?</h2><p class="forgot-copy">Masukkan email akunmu. Kami akan mengirimkan kode verifikasi untuk membantu memulihkan akses.</p><form id="sharedForgotForm"><label class="forgot-field">Email akun<input id="sharedForgotEmail" type="email" autocomplete="email" placeholder="nama@email.com" required></label><p class="forgot-status" id="sharedForgotStatus" role="status"></p><button class="forgot-submit" type="submit">Kirim kode verifikasi</button></form></div></div>`);

    const roleSwitch = document.createElement('div');
    roleSwitch.className = 'role-switch';
    roleSwitch.setAttribute('role', 'group');
    roleSwitch.setAttribute('aria-label', 'Pilih jenis akun');
    roleSwitch.innerHTML = '<button class="role-option active" type="button" data-account-role="student"><strong>Murid / Siswa</strong><span>Belajar dan latihan</span></button><button class="role-option" type="button" data-account-role="teacher"><strong>Guru</strong><span>Mengelola pembelajaran</span></button>';
    document.querySelector('#sharedAccountForm').before(roleSwitch);
    const dashboardRole = document.createElement('span');
    dashboardRole.id = 'sharedDashboardRole';
    dashboardRole.className = 'dashboard-role';
    document.querySelector('#sharedAccountDashboard .dashboard-head .eyebrow').after(dashboardRole);
    const formView = document.querySelector('#sharedAccountFormView');
    const dashboard = document.querySelector('#sharedAccountDashboard');
    const error = document.querySelector('#sharedAccountError');
    const account = () => { try { const saved = JSON.parse(localStorage.getItem('kitaBelajarAccount')); return saved ? { ...saved, role: saved.role || 'student' } : null; } catch (storageError) { return null; } };
    const updateTrigger = () => { const saved = account(); const name = saved ? saved.name.split(' ')[0] : 'Masuk'; const initial = saved ? saved.name.trim().charAt(0).toUpperCase() : '?'; const label = document.querySelector('#accountLabel'); const avatar = document.querySelector('#accountAvatar'); if (label) label.textContent = name; if (avatar) avatar.textContent = initial; if (!label && trigger.tagName === 'A') { trigger.textContent = name; trigger.style.setProperty('--account-initial', `'${initial}'`); } };
    const setMode = mode => { authMode = mode; const signup = mode === 'signup'; document.querySelectorAll('[data-shared-auth]').forEach(tab => tab.classList.toggle('active', tab.dataset.sharedAuth === mode)); document.querySelector('#sharedNameField').hidden = !signup; document.querySelector('#sharedAccountTitle').textContent = signup ? 'Mulai ruang belajarmu.' : 'Selamat datang kembali.'; document.querySelector('#sharedAccountCopy').textContent = signup ? 'Buat akun gratis untuk menyimpan produk dan melihat akses belajarmu.' : 'Masuk untuk menyimpan produk dan melihat akses belajarmu dalam satu tempat.'; document.querySelector('#sharedAccountSubmit').textContent = signup ? 'Buat akun' : 'Masuk ke akun'; document.querySelector('#sharedForgotPassword').hidden = signup; error.textContent = ''; };
    const setRole = role => { accountRole = role; roleSwitch.querySelectorAll('[data-account-role]').forEach(option => option.classList.toggle('active', option.dataset.accountRole === role)); };
    const showDashboard = saved => { formView.style.display = 'none'; dashboard.classList.add('show'); document.querySelector('#sharedDashboardName').textContent = saved.name.split(' ')[0]; dashboardRole.textContent = saved.role === 'teacher' ? 'Dashboard guru' : 'Dashboard murid'; };
    const close = () => { modal.classList.remove('open'); document.body.classList.remove('modal-open'); };
    const open = () => { modal.classList.add('open'); document.body.classList.add('modal-open'); const saved = account(); if (saved) { showDashboard(saved); } else { formView.style.display = 'block'; dashboard.classList.remove('show'); setMode('login'); setRole('student'); document.querySelector('#sharedAccountEmail').focus(); } };
    trigger.addEventListener('click', event => { if (trigger.tagName === 'A') event.preventDefault(); open(); });
    document.querySelector('#sharedCloseAccount').addEventListener('click', close);
    modal.addEventListener('click', event => { if (event.target === modal) close(); });
    document.querySelectorAll('[data-shared-auth]').forEach(tab => tab.addEventListener('click', () => setMode(tab.dataset.sharedAuth)));
    roleSwitch.querySelectorAll('[data-account-role]').forEach(option => option.addEventListener('click', () => setRole(option.dataset.accountRole)));
    document.querySelector('#sharedAccountForm').addEventListener('submit', event => { event.preventDefault(); const email = document.querySelector('#sharedAccountEmail').value.trim().toLowerCase(); const name = document.querySelector('#sharedAccountName').value.trim(); const saved = account(); if (authMode === 'signup' && !name) { error.textContent = 'Nama lengkap perlu diisi.'; return; } if (authMode === 'login' && (!saved || saved.email !== email || saved.role !== accountRole)) { error.textContent = 'Email atau jenis akun tidak sesuai.'; return; } const savedAccount = { name: authMode === 'signup' ? name : (saved.name || email.split('@')[0]), email, role: accountRole }; localStorage.setItem('kitaBelajarAccount', JSON.stringify(savedAccount)); window.dispatchEvent(new Event('kitaBelajarAuthChange')); updateTrigger(); open(); });
    document.querySelector('#sharedLogout').addEventListener('click', () => { localStorage.removeItem('kitaBelajarAccount'); window.dispatchEvent(new Event('kitaBelajarAuthChange')); updateTrigger(); close(); });
    document.querySelector('#sharedForgotPassword').addEventListener('click', () => { document.querySelector('#sharedForgotEmail').value = document.querySelector('#sharedAccountEmail').value.trim().toLowerCase(); document.querySelector('#sharedForgotPanel').classList.add('open'); });
    document.querySelector('#sharedCloseForgot').addEventListener('click', () => document.querySelector('#sharedForgotPanel').classList.remove('open'));
    document.querySelector('#sharedForgotForm').addEventListener('submit', event => { event.preventDefault(); const email = document.querySelector('#sharedForgotEmail').value.trim().toLowerCase(); const status = document.querySelector('#sharedForgotStatus'); if (!account() || account().email !== email) { status.textContent = 'Email belum terdaftar. Periksa kembali alamat emailmu.'; status.className = 'forgot-status error'; return; } status.textContent = `Kode verifikasi telah dikirim ke ${email}.`; status.className = 'forgot-status success'; });
    window.addEventListener('storage', event => { if (event.key !== 'kitaBelajarAccount') return; updateTrigger(); const saved = account(); if (saved) { showDashboard(saved); } else { formView.style.display = 'block'; dashboard.classList.remove('show'); setMode('login'); setRole('student'); } });
    document.querySelectorAll('.access-item strong').forEach((item, index) => { item.textContent = index % 2 ? 'Paket Siap CASN' : 'Paket Lolos UTBK'; });
    updateTrigger();
})();
